# Claude 작업 지침

@AGENTS.md

`AGENTS.md`를 Claude·Codex 공통 지침으로 사용한다. 작업 전에 해당 파일을 읽고 따른다.

## 기록

❌ [2026-08-23] 종단간 OCR 샘플 영상을 만들며 현재 Homebrew FFmpeg에 `drawtext` 필터가 있다고 가정했는데 → 해당 빌드에 필터가 없어 첫 합성 영상 생성이 실패함 → 다음에는 테스트 글자 이미지는 설치된 Pillow로 만들고 FFmpeg는 영상·오디오 합성에만 사용한다.

❌ [2026-08-23] `import Vision` 성공만으로 PyObjC Vision OCR이 동작한다고 판단했는데 → `performRequests_error_`가 오류 없이 `False`를 반환해 실제 인식이 되지 않음 → OCR 런타임은 실제 한글 이미지 인식까지 사전 점검하고, 실패 시 별도 Swift Vision helper로 폴백한다.

❌ [2026-08-23] 더블클릭용 설치 스크립트의 마지막 `read`가 비대화형 검증에서는 EOF로 종료코드 1을 반환했는데 → 의존성 설치가 모두 성공했어도 검증 명령은 실패로 표시됨 → 사용자 입력 대기는 `read ... || true`로 두어 설치 성공 상태를 보존한다.

❌ [2026-08-23] 첫 전체 검증에서 임시 CapCut 초안 루트 경로를 환경변수로 지정하고 폴더를 먼저 만들지 않았는데 → 분석·SRT는 성공했지만 초안 생성 단계가 부분 성공으로 끝남 → 격리된 종단간 검증에서는 입력·출력·초안 루트를 모두 먼저 생성한 뒤 실행한다.

❌ [2026-08-23] 화면의 절대 밝기만으로 플래시를 찾는 첫 분류기가 5초간 유지되는 흰 배경을 플래시로 오인했는데 → 효과 구간이 0~5초로 과도하게 표시됨 → 플래시는 전역 임계값뿐 아니라 앞뒤 프레임의 국소 밝기 대비가 동시에 급증할 때만 판정한다.

❌ [2026-08-23] CapCut 초안의 `참고 원본` 조각을 ffprobe 컨테이너 길이(`analysis.duration`) 기준으로 잘랐는데 → pycapcut이 읽는 소재 길이가 더 짧아(39.775782초 vs 39.75초) 마지막 조각이 "소재 시간 범위 초과" 오류로 초안 생성이 통째로 실패함 → 소재 조각 범위는 `cc.VideoMaterial(...).duration`을 상한으로 잘라서 만든다.

❌ [2026-08-23] pycapcut이 들고 있는 효과·애니메이션 번호가 그대로 열릴 거라 보고 초안에 넣었는데 → 그 번호는 중국판 剪映 기준이라 해외판 CapCut에서 `애니메이션 분실`로 뜨고 효과도 걸리지 않음 → 리소스는 사용자의 기존 CapCut 프로젝트와 `User Data/Cache/effect`에 실제로 내려받힌 것 중에서만 고른다.

❌ [2026-08-23] JSON에 효과 재료가 들어간 것만 확인하고 "CapCut에서 효과가 보인다"고 보고했는데 → 리소스 번호가 이 Mac에 없어서 실제로는 빈 조각이었고 사용자가 화면에서 먼저 발견함 → 초안 검증은 파일 구조가 아니라 이 Mac의 CapCut이 그 리소스를 가지고 있는지까지 확인한다.

❌ [2026-08-23] 자막 스타일을 효과 분석용 256px 썸네일에서 읽으면서 판정 결과를 신뢰했는데 → 작은 말 자막을 OCR이 못 읽어 `|`, `3 *` 같은 잡음을 자막으로 잡았고 색·크기·위치가 모두 틀림 → 자막 스타일은 말이 나오는 시점의 원본 해상도 프레임에서 재고, 반복 등장하는 높이 띠만 말 자막으로 인정한다.

❌ [2026-08-23] 글자색을 OCR 상자 전체 픽셀로 판정했는데 → 따뜻한 조명 배경이 상자를 채워 흰 글자가 `노랑 계열`로 나옴 → 밝기를 두 무리로 갈라 적은 쪽(글자 획)만 골라 색을 잰다.

❌ [2026-08-23] 등장 움직임을 `판별 보류`로 내놓고도 기본값 폴백으로 24개 자막 전부에 팝업 애니메이션을 씌웠는데 → 원본에 없는 움직임이 모든 자막에 똑같이 들어가 레퍼런스와 다른 영상이 됨 → 측정에서 확신이 없으면 기본값을 씌우지 말고 아무것도 적용하지 않는다.

✅ [2026-08-23] 자막 스타일은 OCR로 글자를 읽어서 재려 하면 한글 인식 실패로 무너진다 → "가끔 켜지고 자주 바뀌는 밝은 픽셀"이라는 성질로 자막 띠를 직접 찾으면 글자를 한 자도 못 읽어도 위치·크기·색·외곽선·배경·등장 움직임을 정확히 잴 수 있다 (이 릴스: 세로 57.6%, 높이 2.7%, RGB 249/248/246, 외곽선 없음 — 원본과 일치).

❌ [2026-08-24] 분석 메모(`화면 글자`, `화면 효과 분석`)를 일반 자막 트랙으로 넣어 화면에 그대로 찍히게 했는데 → 편집 화면이 메모 글자로 덮여 정작 재현한 자막·효과가 안 보인다는 지적을 받음 → 메모 성격 트랙은 모두 숨김 처리하고, 화면에 보이는 건 실제 재현물(`말 자막`, `제목 카드`)만 남긴다.

❌ [2026-08-24] 도입부에 밝은 글자가 떠 있는 구간을 통째로 제목 카드로 잡았는데 → 제목(0~2초) 뒤에 인스타 프로필 카드(2~5초)가 이어져 한 덩어리가 됐고 박스 색을 프로필 사진의 인스타 그라데이션에서 뽑아 `#C7664B`로 잘못 냄 → 오버레이는 내용이 크게 바뀌는 지점에서 끊고 맨 앞 덩어리만 제목으로 본다 (고친 뒤 `#AB2518`, 실측 `#AC2517`와 일치).

❌ [2026-08-24] macOS Tesseract의 stderr가 항상 UTF-8이라고 가정했는데 → 일부 경고에 잘못된 바이트가 섞여 `UnicodeDecodeError`로 전체 분석이 중단됨 → 외부 OCR 프로세스 출력은 UTF-8로 읽되 잘못된 바이트를 치환하고, OCR 실패는 다른 분석과 초안 생성을 막지 않게 한다.

❌ [2026-08-24] 노랑 강조와 뒤따르는 흰색 팝업을 같은 CapCut 트랙에 실수 단위로 연달아 넣었는데 → 부동소수점 반올림으로 경계가 1마이크로초 겹쳐 초안 생성이 실패함 → 연속 세그먼트는 정수 마이크로초 경계와 안전 간격을 사용하고 실제 초안 생성까지 검증한다.

✅ [2026-08-24] 완성 MP4에는 편집 레이어가 없으므로 정확한 프리셋 복원은 불가능하지만 → 색 변화, 글자 면적 변화, 프레임 배율, 오버레이 사각형, 오디오 스펙트럼을 각각 시간 이벤트로 분리하면 `노랑→흰색 강조`, `펀치 줌`, `순차 카드`, `효과음 교체 슬롯`을 실제 CapCut 키프레임·교체 트랙으로 재구성할 수 있다.

❌ [2026-08-24] pycapcut이 만든 `common_keyframes` 배열에 값이 있다는 이유로 현재 CapCut에서 자막 팝업이 재생된다고 판단했는데 → 실제 초안의 `material_animations`는 0개였고 키프레임도 `KFTypeScaleX` 하나뿐이라 CapCut 화면에서는 아무 애니메이션도 보이지 않음 → 효과 적용 검증은 JSON 개수 대신 사용자의 정상 프로젝트와 동일한 `material_animations` 리소스 ID·경로·세그먼트 참조가 생성됐는지 확인한다.

❌ [2026-08-24] `노랑→흰색` 이벤트의 두 번째 자막 색을 `event.color.startswith("노랑")`으로 결정했는데 → 흰색 팝업도 노란색으로 만들어짐 → 색 전환은 첫 세그먼트 노랑, 두 번째 세그먼트 흰색을 명시적으로 지정하고 생성 JSON의 실제 RGB까지 검사한다.

✅ [2026-08-24] 이 Mac의 정상 CapCut 프로젝트에서 확인한 로컬 리소스 `팝 업(7145435451946439170)`과 `줌 2(6779083172429697544)`를 같은 `material_animations` 구조로 연결하면 → 자막에는 실제 등장 애니메이션이 생기고 영상은 교체 후에도 세그먼트 효과를 유지한다.

✅ [2026-08-24] Instagram 공개 메타데이터에는 본문·좋아요·댓글 수뿐 아니라 공개 댓글 문구도 포함될 수 있다 → 영상 대본/프레임과 이를 결합하면 편집 분석과 별개로 시의성·3초 후킹·이탈방지·CTA·사랑받은 포인트를 근거와 함께 아카이빙할 수 있다.

❌ [2026-08-24] 큰 한글 제목에 `<br>`만 넣고 왼쪽 컬럼 폭을 제한했는데 → 브라우저가 `기록하고`의 마지막 글자를 다음 줄로 한 번 더 쪼개 제목이 세 줄처럼 보임 → 의미 단위 문장을 각각 블록으로 감싸고 데스크톱에서는 `white-space: nowrap`, 모바일에서는 단어 단위 줄바꿈을 사용한다.

❌ [2026-08-24] Instagram 호스트 여부만 검사해 프로필 URL도 다운로드 작업으로 넘겼는데 → yt-dlp의 긴 영문 추출 오류가 그대로 화면에 노출됨 → `/reel/ID/` 또는 `/p/ID/` 형식을 입력 화면과 서버 양쪽에서 검증하고, 프로필 URL은 공유 링크 복사 방법을 안내하며 즉시 차단한다.

❌ [2026-08-24] 앱 UX 수정 뒤 이 저장소가 pytest를 쓴다고 추정하고 `.venv/bin/pytest`를 실행했는데 → 전용 환경에는 pytest 실행 파일이 없고 실제 검증 명령은 `unittest discover`였음 → 저장소 README의 테스트 명령을 먼저 확인하고 `.venv/bin/python -m unittest discover -s tests -v`로 검증한다.

❌ [2026-08-24] 발행 성과 모달의 기본 날짜를 `new Date().toISOString()`으로 만들었는데 → 브라우저의 UTC 날짜가 한국 날짜보다 하루 빨라 8월 24일에 8월 23일로 표시됨 → 발행·아카이브 날짜 기본값은 `Intl.DateTimeFormat`에 `Asia/Seoul`을 명시해 만든다.

❌ [2026-08-24] HTML ID 중복 검증을 `python -c` 한 줄에서 세미콜론 뒤 `class` 선언으로 작성했는데 → Python 복합문은 그 위치에 올 수 없어 검증 명령 자체가 SyntaxError로 실패함 → 단순 속성 검증은 정규식·Counter 같은 한 줄 표현으로 확인하거나 별도 테스트 함수에 작성한다.

❌ [2026-08-24] 시스템 Python으로 `py_compile`을 실행해 기본 캐시 폴더에 쓰려고 했는데 → 작업공간 밖 `~/Library/Caches` 쓰기가 막혀 코드와 무관한 PermissionError가 발생함 → 로컬 검증은 프로젝트 `.venv`를 쓰고 필요하면 `PYTHONPYCACHEPREFIX`를 `/tmp` 아래로 지정한다.

❌ [2026-08-24] `node -e` 안에서 HTML의 `<script>`를 정규식으로 뽑으며 셸·JavaScript 이중 이스케이프를 잘못했는데 → 정규식 자체가 `Unterminated regexp literal`로 실패함 → 고정 태그 한 개를 읽는 검증은 정규식 대신 `split('<script>')`와 `split('</script>')`를 사용한다.
